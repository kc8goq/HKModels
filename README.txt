This respository contains 3D models for use in Gazebo.

For each model, a SketchUp file is also included.

To use the models, simply copy/paste the folder inside the "Gazebo Model"
directory into your .gazebo/models directory

